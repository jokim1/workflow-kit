{
  "name": "pipelane",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Pipelane — release cockpit for AI vibe coders. Formerly workflow-kit.",
  "bin": {
    "pipelane": "./bin/pipelane",
    "workflow-kit": "./bin/workflow-kit"
  },
  "scripts": {
    "test": "node --test",
    "smoke": "node ./src/cli.ts --help",
    "dashboard": "node ./src/cli.ts dashboard",
    "board": "node ./src/cli.ts board"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
