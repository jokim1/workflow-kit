#!/usr/bin/env node
// Deprecated alias for `bin/pipelane`. Kept as a thin shim for one release
// window so existing `npx workflow-kit ...` invocations and consumer
// package.json bin-name references keep working through the v2.1 rename.
import './pipelane';
